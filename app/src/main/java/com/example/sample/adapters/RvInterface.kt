package com.example.sample.adapters

import com.example.sample.data.Video_files

interface RvInterface {

    fun onImageClickListener(video_url: String)
    fun onUserInfoCLickListener(user_url: String)
}